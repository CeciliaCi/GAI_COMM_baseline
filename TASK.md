# 任务背景
当前 `GAI_COMM` 仓库实现了面向无线信道估计的生成式人工智能方法，其中核心模型为基于 score-based SDE 的扩散模型（Diffusion Model, DM）。仓库中扩散模型以接收导频信号作为条件，通过条件生成方式恢复 MIMO 信道，并用于评估不同场景下的信道估计性能。

为了更加完整地验证 DM 模型的性能，需要引入 `WGAN` 作为生成式神经网络 baseline。`WGAN` 与 DM 同属于生成式模型，可以用于对比不同生成模型在信道估计任务中的重构精度、泛化能力和训练稳定性。

当前需要完成的任务是：在 `GAI_COMM` 仓库已有 DM 训练与测试流程的基础上，适配并训练 `WGAN` baseline，使其能够读取新的 LEO 信道估计数据集，并与 DM 模型在相同数据集、相同 SNR 设置和相同评价指标下进行公平对比。

# 不足之处
当前仓库原始数据集主要面向 DeepMIMO 场景，例如 `O1_28`、`O1_28B`、`I2_28B` 和 `mixed` 等场景。现有训练、测试和配置代码通常默认这些数据集命名方式。

但是当前研究任务已经转向 LEO-NTN 场景，数据集命名规则发生变化。当前代码需要支持三类 LEO 场景：

- `Rural`
- `Urban`
- `DenseUrban`

对应输出数据集命名规则为：

```text
LEO_<Scenario>_seed<Seed>